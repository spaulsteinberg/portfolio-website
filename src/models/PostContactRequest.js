export default class PostContactRequest {
    constructor(fname, lname, email, msg){
        this.firstName = fname;
        this.lastName = lname;
        this.email = email;
        this.msg = msg;
    }
}